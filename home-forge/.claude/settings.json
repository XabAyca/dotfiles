{
  "permissions": {
    "allow": [
      "Bash(cargo test:*)",
      "Bash(cargo build:*)",
      "Bash(cargo check:*)",
      "Bash(cargo clippy:*)",
      "Bash(cargo fmt:*)",
      "Bash(cargo audit:*)"
    ],
    "deny": [
      "Bash(git reset --hard:*)",
      "Bash(sudo:*)",
      "Bash(rm -rf:*)"
    ],
    "ask": [
      "Bash(git push:*)"
    ]
  },
  "enabledPlugins": {
    "ponytail@ponytail": true
  },
  "extraKnownMarketplaces": {
    "ponytail": {
      "source": {
        "source": "github",
        "repo": "DietrichGebert/ponytail"
      }
    }
  },
  "theme": "dark-ansi",
  "autoMode": {
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: assume private unless the remote host and repo name indicate otherwise, or a visibility check in the transcript shows public — not queryable via gh here; origin is github.com:XabAyca/dotfiles.git (inferred, not confirmed)",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: None configured",
      "**Default / protected branches**: master (default branch, per repo facts); protection status not queryable here",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: None configured",
      "**Host containment**: None configured — headless Ubuntu server reached only via SSH, per ~/.claude/CLAUDE.md; assume ordinary developer machine with open internet unless stated otherwise",
      "**Source control**: The trusted repo (github.com:XabAyca/dotfiles) and its remote(s) only (no additional orgs configured)",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar — note: `home-forge/agents/gateway/config.env` is gitignored in this repo, treat as a credentials file",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: dotfiles/system configuration management on a personal headless Ubuntu server (per ~/.claude/CLAUDE.md)",
      "**Trusted repo**: /home/dev/projects/dotfiles/main (this repo), remote github.com:XabAyca/dotfiles.git — visibility unconfirmed, treat as private by default",
      "**Org-specific CLIs**: None configured — mise (toolchain manager), stow (dotfile symlink manager) used routinely in this repo per shell history and project usage",
      "routine under ~/projects/dotfiles/main/ prefix: stow, mise, git operations scoped to this repo"
    ],
    "soft_deny": [
      "$defaults",
      "Bash(rm:~/.ssh/*)",
      "Bash(chsh:*)"
    ]
  }
}
